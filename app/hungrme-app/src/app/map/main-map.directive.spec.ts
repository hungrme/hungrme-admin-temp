import { MainMapDirective } from './main-map.directive';

describe('MainMapDirective', () => {
  it('should create an instance', () => {
    const directive = new MainMapDirective();
    expect(directive).toBeTruthy();
  });
});
